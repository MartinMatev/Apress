@model string[]

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section Header{
    <div class="View">
        @foreach (string str in new[] { "Home", "List", "Edit" })
        {
            @Html.ActionLink(str, str, null, new { style = "margin: 5px" });
        }
    </div>
}

@*This below goes to the @RenderBody*@
<div class="View">
    This is a list of fruit names:
    @foreach (string name in Model)
    {
        <span><b>@name</b></span>
    }
</div>
@* up untill here *@

@section Footer{
    <div class="View">
        This is the footer.
    </div>
}